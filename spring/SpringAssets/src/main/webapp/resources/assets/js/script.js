/**
 * add elements to container when document is ready
 */
$( document ).ready(function() {
	$('#container').append("<h1>Spring Web MVC Project with Assets</h1>");
	$('#container').append("<p>Assets contain .css and .js files</p>");
});
